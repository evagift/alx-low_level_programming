#include "main.h"
/**
 * _abs - Checking  point
 * @number: the integer to compute
 * Return: the value of num
 */
int _abs(int number)
{
	if (number < 0)
	{
		return (-number);
	}
	else
	{
		return (number);
	}
}
